apiVersion: v1
kind: Pod
metadata:
  name: hello-kubernetes
  labels:
    app: hello-kubernetes
spec:
  restartPolicy: Never
  containers:
    - name: hello
      image: busybox:1.36
      command: ["sh", "-c", "echo 'Hello from Kubernetes!'"]
