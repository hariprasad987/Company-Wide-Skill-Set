# Tour Site — Stimulated Go Task

A responsive tour-company homepage served by a small Go web server.

## Run

```bash
cd "4 - 7 yrs/GoLang - Stimulated Task/Tour-Site"
go run main.go
```

Open <http://localhost:8080> in a browser.

The project uses only Go's standard library, so no additional dependencies are required.
